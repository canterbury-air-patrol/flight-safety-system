[pytest]
testpaths = .
addopts = -v --tb=short --strict-markers
timeout = 60
markers =
    slow: long-running (>30s) tests
    requires_docker: needs docker + the postgis/postgis image reachable
    satisfies(*test_ids): declare master-plan test IDs (TC-...) satisfied by a test
